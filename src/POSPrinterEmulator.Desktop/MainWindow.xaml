<Window x:Class="POSPrinterEmulator.Desktop.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:wv2="clr-namespace:Microsoft.Web.WebView2.Wpf;assembly=Microsoft.Web.WebView2.Wpf"
        Title="POS Printer Emulator"
        Icon="Branding/pos-printer-emulator-icon.png"
        Width="1280"
        Height="820"
        MinWidth="980"
        MinHeight="680"
        WindowState="Maximized"
        WindowStartupLocation="CenterScreen"
        Background="#F7F9FC">
  <Grid>
    <wv2:WebView2 x:Name="Browser" Visibility="Hidden" />

    <Grid x:Name="LoadingPanel" Background="#F7F9FC">
      <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center" Width="560">
        <Image Source="Branding/pos-printer-emulator-logo.png"
               Width="500"
               Height="250"
               Stretch="Uniform" />
        <TextBlock x:Name="LoadingMessage"
                   Text="Starting the local printer service…"
                   Foreground="#52647B"
                   FontFamily="Segoe UI"
                   FontSize="15"
                   TextAlignment="Center"
                   Margin="0,8,0,18" />
        <ProgressBar IsIndeterminate="True" Height="4" Foreground="#00B7D9" Background="#DFE7F0" />
      </StackPanel>
    </Grid>

    <Grid x:Name="ErrorPanel" Background="#F7F9FC" Visibility="Collapsed">
      <Border Width="520" Padding="36" Background="White" BorderBrush="#D8E0EA" BorderThickness="1" CornerRadius="14"
              HorizontalAlignment="Center" VerticalAlignment="Center">
        <StackPanel>
          <Image Source="Branding/pos-printer-emulator-icon.png" Width="92" Height="92" Stretch="UniformToFill" />
          <TextBlock Text="POS Printer Emulator could not start"
                     FontFamily="Segoe UI Semibold" FontSize="22" Foreground="#102A50"
                     TextAlignment="Center" Margin="0,22,0,10" />
          <TextBlock x:Name="ErrorMessage" TextWrapping="Wrap" FontFamily="Segoe UI" FontSize="14"
                     Foreground="#52647B" TextAlignment="Center" LineHeight="22" />
          <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,24,0,0">
            <Button Content="Try again" Click="Retry_Click" Background="#0A67C7" Foreground="White" BorderThickness="0" Margin="0,0,10,0" />
            <Button Content="Open in browser" Click="OpenBrowser_Click" Background="White" Foreground="#0A67C7" BorderBrush="#A9C7E7" />
          </StackPanel>
        </StackPanel>
      </Border>
    </Grid>
  </Grid>
</Window>
